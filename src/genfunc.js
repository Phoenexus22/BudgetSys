import { randomInt } from "firebase-tools/lib/utils";

function randId(sourceArray = [])
{
    let id = (Math.random() + 1).toString(64).substring(7)
    randomInt()
    for(let i = 0; i < sourceArray.length; i++)
    {

    }
}